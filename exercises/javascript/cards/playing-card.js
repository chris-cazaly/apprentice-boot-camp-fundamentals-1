class PlayingCard {
  constructor(suit, faceValue) {
    this.suit = suit;
    this.faceValue = faceValue;
  }

  // getCard() {
  //     // di
  //     //makeCard()?
  // }
}

module.exports = {PlayingCard};
